#ifndef _MAIN_H_
#define _MAIN_H_

#include <stdio.h>
#include <stdlib.h>
#include <stdio.h>
#include <stdlib.h>
#include <sys/wait.h>
#include <unistd.h>
#include <string.h>

char **split_str(char *str);
char *prompt(void);
char *_strdup(const char *src);
int _strcmp(char *s1, char *s2);

#endif
